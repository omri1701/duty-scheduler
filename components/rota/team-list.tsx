'use client';
import {useRef,useState} from 'react';
import {GripVertical} from 'lucide-react';
import {Checkbox} from '@/components/ui/checkbox';
import {useHoldDrag} from '@/hooks/use-hold-drag';
import {ink,type Engineer} from '@/lib/rota/engine';
export function TeamList({team,monthTotals,historyTotals,lang,onReorder,onActive}:{team:Engineer[];monthTotals:Record<string,number>;historyTotals:Record<string,number>;lang:'en'|'he';onReorder:(team:Engineer[])=>void;onActive:(id:string,active:boolean)=>void}){
 const t=(a:string,b:string)=>lang==='en'?a:b,source=useRef<string|null>(null);
 const [dragging,setDragging]=useState<string|null>(null),[over,setOver]=useState<string|null>(null),[announcement,setAnnouncement]=useState('');
 function move(from:string,to:string){const a=team.findIndex(p=>p.id===from),b=team.findIndex(p=>p.id===to);if(a<0||b<0||a===b)return;const next=[...team];next.splice(b,0,next.splice(a,1)[0]);onReorder(next);setAnnouncement(`${team[a].name} · ${b+1}`)}
 function clear(){source.current=null;setDragging(null);setOver(null)}
 const gesture=useHoldDrag({enabled:true,onStart:id=>{source.current=id;setDragging(id)},onMove:setOver,onEnd:id=>{if(id&&source.current)move(source.current,id);clear()},onCancel:clear});
 return <><p className="gesture-hint">{t('Hold & drag to reorder · Senior → newest','לחיצה ארוכה וגרירה לשינוי הסדר · ותיק ← חדש')}</p><span className="sr-only" aria-live="polite">{announcement}</span><div ref={gesture.container} className="team-list gesture-grid">{team.map((p,i)=><div data-gesture-key={p.id} key={p.id} className={['team-row',!p.active?'inactive':'',dragging===p.id?'drag-source':'',over===p.id&&dragging!==p.id?'drag-target':''].join(' ')}><button className="team-grip" aria-label={`${t('Drag to reorder','גרירה לשינוי הסדר')} ${p.name}`} aria-describedby="team-keyboard-hint" onKeyDown={e=>{if(e.key==='ArrowUp'||e.key==='ArrowDown'){e.preventDefault();const target=team[i+(e.key==='ArrowUp'?-1:1)];if(target)move(p.id,target.id)}}}><GripVertical size={18}/><span>{i+1}</span></button><span className="avatar" style={{background:p.color,color:ink(p.color)}}>{p.name.split(' ').map(n=>n[0]).join('').slice(0,2)}</span><div className="team-name"><strong>{p.name}</strong><small>{p.id==='e0'?t('Manager + engineer','מנהל ומהנדס'):i===team.length-1?t('Newest teammate','חבר הצוות החדש ביותר'):t('Engineer','מהנדס')}</small></div><div className="team-points"><strong>{monthTotals[p.id]??0}</strong><span>{t('this month','החודש')}</span></div><div className="team-points total-points"><strong>{historyTotals[p.id]??0}</strong><span>{t('all months','כל החודשים')}</span></div><label className="active-checkbox" onPointerDown={e=>e.stopPropagation()} onTouchStart={e=>e.stopPropagation()}><Checkbox checked={p.active} onCheckedChange={v=>onActive(p.id,!!v)}/>{t('Active','פעיל')}</label></div>)}</div><span id="team-keyboard-hint" className="sr-only">{t('Use up and down keys to reorder.','אפשר להשתמש במקשי למעלה ולמטה לשינוי הסדר.')}</span></>
}
